import {getAccessToken,getState,initAuth,signOut,supabaseRpc} from './auth-client.js';
import {qrSvg} from './qr-v6.js';

const TEMPLATE=`
<section class="admin-route" aria-label="Internal administration">
  <div class="admin-shell">
    <header class="admin-header"><div class="admin-brand"><img src="/assets/brand/sindhorn-midtown-vignette-black.png" alt="Sindhorn Midtown Hotel Bangkok, Vignette Collection"><div><div class="admin-title">Internal Admin</div><div id="adminIdentity" class="admin-sub">Checking access…</div></div></div><div class="admin-actions"><a class="chip-btn" href="/" data-app-route="today">App</a><button id="signOutButton" class="chip-btn" type="button">Sign out</button></div></header>
    <nav class="admin-nav" aria-label="Admin sections"><button type="button" data-panel="users" aria-selected="true">Users</button><button type="button" data-panel="broadcasts" aria-selected="false">Broadcasts</button><button type="button" data-panel="departments" aria-selected="false">Departments &amp; Groups</button><button type="button" data-panel="audit" aria-selected="false">Audit Log</button><button type="button" data-panel="settings" aria-selected="false">Settings</button></nav>
    <section id="accessPanel" class="admin-panel hidden"><div class="empty"><strong>Administrator access required.</strong><br><span class="thai">ต้องใช้บัญชีผู้ดูแลระบบ</span><br><br><a href="/account" data-app-route="account">Open my account / บัญชีของฉัน</a></div></section>
    <section id="usersPanel" class="admin-panel"><div class="panel-head"><div><div class="panel-title">Employees</div><div class="panel-note thai">พนักงานและสิทธิ์การใช้งาน</div></div><div class="toolbar"><input id="userSearch" class="search" type="search" placeholder="Search employee…" aria-label="Search employees"><button id="addUserButton" class="add-btn" type="button">Add employee</button></div></div><div class="security-banner">Employee records, private contacts and one-time codes are handled through role-gated Supabase RPCs. Private contact tables and privileged keys are never exposed directly to the browser.</div><div id="userList" class="user-list"><div class="empty">Loading employees…</div></div></section>
    <section id="placeholderPanel" class="admin-panel hidden"><div class="empty" id="placeholderText"></div></section>
  </div>
  <dialog id="userDialog"><form id="userForm" method="dialog" class="dialog-body"><div class="dialog-head"><div id="dialogTitle" class="dialog-title">Add employee</div><button id="dialogClose" class="close-btn" type="button" aria-label="Close">×</button></div><input id="employeeIdHidden" type="hidden"><div class="form-grid"><div class="field"><label for="employeeNumber">Employee ID</label><input id="employeeNumber" maxlength="64" required autocomplete="off"></div><div class="field"><label for="displayName">Display name</label><input id="displayName" maxlength="160"></div><div class="field full"><label for="workEmail">Hotel work email <span class="optional">(optional)</span></label><input id="workEmail" type="email" maxlength="320" placeholder="name@ihg.com"></div><div class="field"><label for="department">Department</label><select id="department"><option value="">Unassigned</option></select></div><div class="field"><label for="role">Role</label><select id="role"><option value="employee">Employee</option><option value="supervisor">Supervisor</option><option value="manager">Manager</option><option value="admin">Admin</option><option value="super_admin">Super Admin</option></select></div><div class="field"><label for="accountType">Account type</label><select id="accountType"><option value="employee">Employee</option><option value="developer">Developer</option><option value="contractor">Contractor</option><option value="service">Service</option></select></div><div class="field"><label for="preferredLanguage">Preferred language</label><select id="preferredLanguage"><option value="th">ไทย</option><option value="en">English</option></select></div><div class="field"><label for="active">Access</label><select id="active"><option value="true">Active</option><option value="false">Inactive</option></select></div><div class="field full contact-heading"><div class="field-section-title">Private contact <span class="optional">(admin only)</span></div><div class="note">Stored in the private Sindhorn schema. These details are not used for automated login.</div></div><div class="field"><label for="personalEmail">Personal email <span class="optional">(optional)</span></label><input id="personalEmail" type="email" maxlength="320" autocomplete="off"></div><div class="field"><label for="mobileE164">Mobile <span class="optional">(optional, E.164)</span></label><input id="mobileE164" inputmode="tel" maxlength="16" placeholder="+66…" autocomplete="off"></div></div><div id="dialogStatus" class="status" role="status" aria-live="polite"></div><div class="dialog-actions dialog-actions-split"><div class="dialog-danger-actions"><button id="revokeButton" class="danger hidden" type="button">Revoke access &amp; sessions</button></div><div class="dialog-primary-actions"><button id="activationButton" class="hidden" type="button">Issue first-login code</button><button id="cancelButton" type="button">Cancel</button><button id="saveButton" class="save" type="submit">Save</button></div></div></form></dialog>
  <dialog id="codeDialog"><div class="dialog-body"><div class="dialog-head"><div id="codeDialogTitle" class="dialog-title">One-time sign-in code</div><button id="codeClose" class="close-btn" type="button" aria-label="Close">×</button></div><p class="support">Give the code directly to the employee, or let them scan the QR. It expires after 15 minutes and is stored only as a bcrypt hash.</p><div id="issuedCode" class="issued-code"></div><div id="issuedCodeMeta" class="note"></div><div id="inviteQr" class="invite-qr" aria-live="polite"></div><div class="code-actions"><button id="copyCode" type="button">Copy code</button><button id="copyInvite" type="button">Copy invitation link</button><button id="shareInvite" type="button" class="hidden">Share</button></div><div class="note">The invitation uses a URL fragment so the one-time code is not sent to the web server. The login page removes the fragment from the address bar immediately after reading it.</div><div class="dialog-actions"><button id="codeDone" class="save" type="button">Done</button></div></div></dialog>
</section>`;

function esc(value){return String(value??'').replace(/[&<>'"]/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[char]))}
function roleLabel(role){return String(role||'employee').replaceAll('_',' ').replace(/\b\w/g,c=>c.toUpperCase())}

export async function mountAdminRoute(root){
  root.innerHTML=TEMPLATE;
  const $=selector=>root.querySelector(selector),$$=selector=>[...root.querySelectorAll(selector)];
  let users=[],departments=[],actor=null,currentUser=null,currentInviteUrl='',disposed=false;const cleanupFns=[];
  const on=(element,event,handler,options)=>{element?.addEventListener(event,handler,options);cleanupFns.push(()=>element?.removeEventListener(event,handler,options))};
  function identityLabel(user){const methods=(user.sindhorn_employee_identities||[]).map(item=>item.login_method);if(methods.includes('microsoft365')&&methods.includes('employee_id'))return'Microsoft 365 + Employee ID';if(methods.includes('microsoft365'))return'Microsoft 365';if(methods.includes('employee_id')||user.auth_user_id)return'Employee ID';return'Not activated'}
  function departmentLabel(user){if(!user.department_id)return'Unassigned';const item=departments.find(department=>department.id===user.department_id);return item?.name_en||item?.code||'Department'}
  function showStatus(element,message,tone='neutral'){if(!element)return;element.textContent=message||'';element.dataset.show=String(Boolean(message));element.dataset.tone=tone}
  async function rpc(name,params={}){const token=getAccessToken();if(!token)throw Object.assign(new Error('authentication_required'),{code:'authentication_required'});try{return await supabaseRpc(name,params,{accessToken:token})}catch(error){const message=String(error?.message||'').toLowerCase(),providerCode=String(error?.payload?.code||'');if(message.includes('admin access required'))error.code='admin_access_required';else if(message.includes('insufficient role'))error.code='insufficient_role';else if(message.includes('cannot remove own admin access'))error.code='cannot_remove_own_admin_access';else if(providerCode==='23505'||message.includes('duplicate key'))error.code='employee_already_exists';else if(message.includes('invalid employee input'))error.code='invalid_employee_input';else if(message.includes('invalid department'))error.code='invalid_department';else if(message.includes('invalid personal email'))error.code='invalid_personal_email';else if(message.includes('invalid mobile'))error.code='invalid_mobile';else error.code=error.code||'request_failed';throw error}}
  function writeError(error){if(error?.code==='admin_access_required'||error?.code==='authentication_required')return'Administrator sign-in is required.';if(error?.code==='insufficient_role')return'Your administrator role cannot make this access change.';if(error?.code==='cannot_remove_own_admin_access')return'You cannot remove your own administrator access.';if(error?.code==='employee_already_exists')return'That Employee ID or hotel email is already assigned.';if(error?.code==='invalid_employee_input')return'Check the employee details and try again.';if(error?.code==='invalid_department')return'Choose an active department or leave it unassigned.';if(error?.code==='invalid_personal_email')return'Check the personal email address.';if(error?.code==='invalid_mobile')return'Use an international mobile number such as +66…';return'The change could not be saved. Please try again.'}
  function renderUsers(){if(disposed)return;const q=$('#userSearch').value.trim().toLowerCase();const filtered=users.filter(user=>[user.employee_number,user.display_name,user.work_email,user.role,user.account_type,departmentLabel(user)].some(value=>String(value||'').toLowerCase().includes(q)));$('#userList').innerHTML=filtered.length?filtered.map(user=>`<article class="user-row" data-user-id="${esc(user.id)}"><div><div class="user-name">${esc(user.display_name||user.employee_number)}</div><div class="user-id">${esc(user.employee_number)}${user.work_email?` · ${esc(user.work_email)}`:''}</div></div><div><span class="pill">${esc(roleLabel(user.role))}</span><div class="user-login">${esc(user.account_type||'employee')}</div></div><div><span class="pill ${user.active?'':'inactive'}">${user.active?'Active':'Inactive'}</span><div class="user-login">${esc(user.preferred_language==='en'?'English':'ไทย')}</div></div><div><div class="user-login">${esc(departmentLabel(user))}</div><div class="user-login">${esc(identityLabel(user))}${Number(user.session_count||0)>0?` · ${Number(user.session_count)} session${Number(user.session_count)===1?'':'s'}`:''}</div></div><button class="row-edit" type="button" data-edit="${esc(user.id)}">Edit</button></article>`).join(''):'<div class="empty">No employees match this search.</div>';$$('[data-edit]').forEach(button=>on(button,'click',()=>openUserDialog(users.find(user=>user.id===button.dataset.edit))))}
  async function loadUsers(){if(disposed)return;$('#userList').innerHTML='<div class="empty">Loading employees…</div>';try{const result=await rpc('sindhorn_admin_list_users_v3');if(disposed)return;users=result?.users||[];departments=result?.departments||[];actor=result?.actor||null;renderUsers()}catch(error){if(!disposed)$('#userList').innerHTML=`<div class="empty">Could not load employee directory.<br><small>${esc(error.code||error.message)}</small></div>`}}
  function renderDepartmentOptions(selected=''){const options=['<option value="">Unassigned</option>',...departments.filter(item=>item.active!==false||item.id===selected).map(item=>`<option value="${esc(item.id)}">${esc(item.name_en||item.code)}${item.active===false?' (inactive)':''}</option>`)];$('#department').innerHTML=options.join('');$('#department').value=selected||''}
  function fillDialog(user){currentUser=user||null;$('#dialogTitle').textContent=user?'Edit employee':'Add employee';$('#employeeIdHidden').value=user?.id||'';$('#employeeNumber').value=user?.employee_number||'';$('#displayName').value=user?.display_name||'';$('#workEmail').value=user?.work_email||'';renderDepartmentOptions(user?.department_id||'');$('#role').value=user?.role||'employee';$('#accountType').value=user?.account_type||'employee';$('#preferredLanguage').value=user?.preferred_language||'th';$('#active').value=String(user?.active!==false);const contactReadable=!user||user.private_contact!==null;$('#personalEmail').disabled=!contactReadable;$('#mobileE164').disabled=!contactReadable;$('#personalEmail').value=contactReadable?(user?.private_contact?.personal_email||''):'';$('#mobileE164').value=contactReadable?(user?.private_contact?.mobile_e164||''):'';$('#activationButton').classList.toggle('hidden',!user||user.active===false);$('#activationButton').textContent=user?.auth_user_id?'Issue recovery code':'Issue first-login code';$('#revokeButton').classList.toggle('hidden',!user||user.active===false||user.id===actor?.id);showStatus($('#dialogStatus'),contactReadable?'':'Private contact details for this privileged account require Super Admin access.',contactReadable?'neutral':'error');const isSuper=actor?.role==='super_admin';[...$('#role').options].forEach(option=>{option.disabled=!isSuper&&['admin','super_admin'].includes(option.value)});[...$('#accountType').options].forEach(option=>{option.disabled=!isSuper&&option.value==='developer'})}
  function openUserDialog(user=null){fillDialog(user);$('#userDialog').showModal()}
  function closeUserDialog(){if($('#userDialog').open)$('#userDialog').close()}
  function formPayload(){return{employeeId:$('#employeeIdHidden').value||undefined,employeeNumber:$('#employeeNumber').value.trim(),displayName:$('#displayName').value.trim(),workEmail:$('#workEmail').value.trim(),departmentId:$('#department').value||null,role:$('#role').value,accountType:$('#accountType').value,preferredLanguage:$('#preferredLanguage').value,active:$('#active').value==='true',personalEmail:$('#personalEmail').value.trim(),mobileE164:$('#mobileE164').value.trim()}}
  function rpcPayload(payload,{editing=false}={}){return{...(editing?{p_employee_id:payload.employeeId}:{}),p_employee_number:payload.employeeNumber,p_display_name:payload.displayName||null,p_work_email:payload.workEmail||null,p_department_id:payload.departmentId||null,p_role:payload.role,p_active:payload.active,p_preferred_language:payload.preferredLanguage,p_account_type:payload.accountType}}
  async function copyText(value){try{await navigator.clipboard.writeText(value);return true}catch(_){}const area=document.createElement('textarea');area.value=value;area.setAttribute('readonly','');area.style.position='fixed';area.style.opacity='0';document.body.append(area);area.select();const ok=document.execCommand('copy');area.remove();return ok}
  function invitationUrl(employeeNumber,code){return`${location.origin}/login.html#i=${encodeURIComponent(employeeNumber)}&c=${encodeURIComponent(code)}`}
  function showIssuedCode(result){const code=String(result.code||''),purpose=result.purpose==='recovery'?'Recovery':'First login';currentInviteUrl=invitationUrl(result.employeeNumber,code);$('#codeDialogTitle').textContent=`${purpose} code`;$('#issuedCode').textContent=code;$('#issuedCodeMeta').textContent=`Employee ${result.employeeNumber} · ${purpose} · expires ${new Date(result.expiresAt).toLocaleTimeString([],{hour:'2-digit',minute:'2-digit'})}`;try{$('#inviteQr').innerHTML=qrSvg(currentInviteUrl)}catch(_){$('#inviteQr').textContent='QR unavailable. Use the code or copy the invitation link.'}$('#shareInvite').classList.toggle('hidden',typeof navigator.share!=='function');$('#codeDialog').showModal()}
  on($('#userForm'),'submit',async event=>{event.preventDefault();const editing=Boolean($('#employeeIdHidden').value),save=$('#saveButton');save.disabled=true;showStatus($('#dialogStatus'),'Saving…');try{const payload=formPayload();const result=await rpc(editing?'sindhorn_admin_update_employee_v2':'sindhorn_admin_create_employee_v2',rpcPayload(payload,{editing}));const employee=result?.employee;if(!employee?.id)throw new Error('employee_save_failed');if(!$('#personalEmail').disabled){try{await rpc('sindhorn_admin_upsert_contact_v2',{p_employee_id:employee.id,p_personal_email:payload.personalEmail||null,p_mobile_e164:payload.mobileE164||null})}catch(contactError){showStatus($('#dialogStatus'),`Employee details were saved, but the private contact was not: ${writeError(contactError)}`,'error');await loadUsers();return}}closeUserDialog();await loadUsers()}catch(error){showStatus($('#dialogStatus'),writeError(error),'error')}finally{save.disabled=false}});
  on($('#activationButton'),'click',async()=>{if(!currentUser)return;const button=$('#activationButton');button.disabled=true;showStatus($('#dialogStatus'),'Issuing one-time code…');try{const result=await rpc('sindhorn_admin_issue_activation_code_v2',{p_employee_number:currentUser.employee_number});closeUserDialog();showIssuedCode(result)}catch(error){showStatus($('#dialogStatus'),writeError(error),'error')}finally{button.disabled=false}});
  on($('#revokeButton'),'click',async()=>{if(!currentUser)return;if(!confirm(`Revoke access for ${currentUser.display_name||currentUser.employee_number}?\n\nThis immediately marks the employee inactive, revokes unused codes and ends refreshable Supabase sessions.`))return;const button=$('#revokeButton');button.disabled=true;showStatus($('#dialogStatus'),'Revoking access…');try{await rpc('sindhorn_admin_revoke_access_v2',{p_employee_id:currentUser.id});closeUserDialog();await loadUsers()}catch(error){showStatus($('#dialogStatus'),writeError(error),'error')}finally{button.disabled=false}});
  on($('#copyCode'),'click',async()=>{const ok=await copyText($('#issuedCode').textContent.trim());$('#copyCode').textContent=ok?'Copied':'Copy failed';setTimeout(()=>{if(!disposed)$('#copyCode').textContent='Copy code'},1400)});on($('#copyInvite'),'click',async()=>{const ok=await copyText(currentInviteUrl);$('#copyInvite').textContent=ok?'Copied':'Copy failed';setTimeout(()=>{if(!disposed)$('#copyInvite').textContent='Copy invitation link'},1400)});on($('#shareInvite'),'click',async()=>{if(!currentInviteUrl||typeof navigator.share!=='function')return;try{await navigator.share({title:'Sindhorn Midtown employee sign in',text:'One-time employee sign-in invitation',url:currentInviteUrl})}catch(_){}});
  on($('#addUserButton'),'click',()=>openUserDialog());on($('#dialogClose'),'click',closeUserDialog);on($('#cancelButton'),'click',closeUserDialog);on($('#userSearch'),'input',renderUsers);on($('#codeClose'),'click',()=>$('#codeDialog').close());on($('#codeDone'),'click',()=>$('#codeDialog').close());on($('#signOutButton'),'click',async()=>{await signOut();location.replace('/login.html')});
  $$('.admin-nav button').forEach(button=>on(button,'click',()=>{$$('.admin-nav button').forEach(node=>node.setAttribute('aria-selected',String(node===button)));const usersSelected=button.dataset.panel==='users';$('#usersPanel').classList.toggle('hidden',!usersSelected);$('#placeholderPanel').classList.toggle('hidden',usersSelected);if(!usersSelected)$('#placeholderText').innerHTML=`<strong>${esc(button.textContent)}</strong><br><br>This section is reserved in the Phase 9 admin architecture and will be connected next.`}));
  try{await initAuth()}catch(_){}
  if(disposed)return()=>{};const state=getState(),profile=state.profile;
  if(!state.authenticated){location.replace(`/login.html?next=${encodeURIComponent('/admin')}`);return()=>{}}
  if(!['admin','super_admin'].includes(profile?.role)){$('#usersPanel').classList.add('hidden');$('#accessPanel').classList.remove('hidden');$('#adminIdentity').textContent='Administrator access required'}else{$('#adminIdentity').textContent=`${profile.display_name||profile.employee_number} · ${roleLabel(profile.role)}`;await loadUsers()}
  return()=>{disposed=true;cleanupFns.splice(0).forEach(fn=>fn());$$('dialog[open]').forEach(dialog=>{try{dialog.close()}catch(_){}});currentInviteUrl=''};
}
