import {BETTA_DAY_PERIODS} from './betta-day-periods.js';

const esc=value=>String(value??'').replace(/[&<>"']/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]));
export function mountBettaDayCycleControls(route){
  const env=window.SindhornEnvironment;if(!route||!env?.getState||!env?.setBettaPeriod)return()=>{};
  const host=document.createElement('div');host.className='ci-specimen';host.dataset.ciBettaDayCycle='true';host.style.marginTop='9px';
  host.innerHTML=`<div class="ci-specimen-label"><span>Betta Day Cycle</span><span>Asia/Bangkok · developer preview</span></div><div class="ci-specimen-stage"><dl class="ci-fact-list"><div><dt>Bangkok</dt><dd data-betta-clock>—</dd></div><div><dt>Period</dt><dd data-betta-period-readout>—</dd></div><div><dt>Betta</dt><dd data-betta-baseline>—</dd></div><div><dt>Transition</dt><dd data-betta-mix>—</dd></div></dl><div class="ci-chip-row" data-betta-periods>${BETTA_DAY_PERIODS.map(period=>`<button class="fnb-chip ci-betta-period-chip" type="button" data-betta-period="${esc(period.key)}">${esc(period.name)}</button>`).join('')}</div><div class="ci-actions" style="margin-top:12px"><button class="settings-primary" type="button" data-betta-live>Live Bangkok Time</button><button class="app-quiet-action" type="button" data-betta-preview><span>Preview 24h · 3 min</span></button></div><p class="ci-note">Clock selects biological identity. Himawari-9 remains the live environmental modifier. Natural rollovers morph for 60 seconds; developer pins and foreground corrections use 900 ms.</p></div>`;
  const motion=route.querySelector('#ci-motion')||route.querySelector('[data-ci-section="motion"]'),anchor=motion?.querySelector('.ci-specimen:last-child');if(anchor)anchor.insertAdjacentElement('afterend',host);else motion?.append(host);
  const listeners=[];const on=(node,event,fn)=>{node?.addEventListener(event,fn);listeners.push(()=>node?.removeEventListener(event,fn))};
  host.querySelectorAll('button[data-betta-period]').forEach(button=>on(button,'click',()=>env.setBettaPeriod(button.dataset.bettaPeriod)));
  on(host.querySelector('[data-betta-live]'),'click',()=>env.useLiveBettaDayCycle());
  on(host.querySelector('[data-betta-preview]'),'click',()=>env.previewBettaDayCycle(180));
  const paint=()=>{if(!host.isConnected)return;const d=env.getState()?.betta?.dayCycle||{};host.querySelector('[data-betta-clock]').textContent=d.bangkokTime||'—';host.querySelector('[data-betta-period-readout]').textContent=d.targetPeriodKey?`${d.targetPeriodKey} · ${d.mode}`:'—';host.querySelector('[data-betta-baseline]').textContent=d.targetBaseline||'—';host.querySelector('[data-betta-mix]').textContent=`${Math.round((Number(d.transitionMix)||0)*100)}% · ${Math.round(Number(d.transitionDurationMs)||0)} ms`;host.querySelectorAll('button[data-betta-period]').forEach(button=>button.classList.toggle('is-active',button.dataset.bettaPeriod===d.targetPeriodKey))};
  paint();const timer=setInterval(paint,250);return()=>{clearInterval(timer);listeners.splice(0).forEach(fn=>fn());host.remove()};
}
