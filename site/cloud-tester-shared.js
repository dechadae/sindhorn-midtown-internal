import * as THREE from './vendor/three.module.js';
import {bangkokSeasonalProfile,cloudMorphologyForWeather,seasonalSkyForState} from './seasonal-sky.js';
import {ATMOSPHERE_VERTEX_SHADER,ATMOSPHERE_FRAGMENT_SHADER,createAtmosphereNoiseTexture} from './atmosphere-shader.js';

const clamp=(v,a=0,b=1)=>Math.min(b,Math.max(a,Number(v)||0));
const vec3=a=>new THREE.Vector3(a[0],a[1],a[2]);
const WMO={clear:0,'mainly-clear':1,'partly-cloudy':2,overcast:3,rain:63,showers:81,thunderstorm:95};
const defaults={solarAltitude:-3.3,solarAzimuth:249,seasonal:1,haze:.18,warmLight:.92,cloudCover:.04,humidity:.58,highCoverage:.008,highOpacity:.28,highScale:1.15,highStretch:4.4,highSpeed:.42,midCoverage:.005,midOpacity:.5,midScale:1,midDetail:1,midSoftness:.11,midEdgeLight:.62,lowCoverage:0,lowOpacity:.82,lowScale:.85,lowBuild:1,connected:0,baseDarkness:.22,lightLeaks:.52,contrast:1.15,windSpeed:8,windDirection:220};
const presets={
 janClear:{date:'2026-01-15',weather:'clear',state:{...defaults}},
 janBroken:{date:'2026-01-15',weather:'partly-cloudy',state:{...defaults,cloudCover:.48,humidity:.62,highCoverage:.22,highOpacity:.42,midCoverage:.58,midOpacity:.80,midScale:.88,midDetail:1.35,midSoftness:.09,midEdgeLight:.92,lowCoverage:.03,baseDarkness:.26,connected:.04,lightLeaks:.72}},
 aprHaze:{date:'2026-04-10',weather:'mainly-clear',state:{...defaults,solarAltitude:2.5,solarAzimuth:278,haze:.62,warmLight:1.08,cloudCover:.12,humidity:.52,highCoverage:.12,highOpacity:.22,midCoverage:.10,midOpacity:.48,midScale:1.15,midDetail:.82,baseDarkness:.26,windSpeed:5}},
 augPartly:{date:'2026-08-18',weather:'partly-cloudy',state:{...defaults,solarAltitude:6,solarAzimuth:276,haze:.44,warmLight:.68,cloudCover:.56,humidity:.82,highCoverage:.10,highOpacity:.25,midCoverage:.58,midOpacity:.84,midScale:.78,midDetail:1.18,midSoftness:.10,midEdgeLight:.62,lowCoverage:.28,lowOpacity:.92,lowScale:.72,lowBuild:1.45,connected:.18,baseDarkness:.62,lightLeaks:.72,windSpeed:15,windDirection:235}},
 augStorm:{date:'2026-08-18',weather:'thunderstorm',state:{...defaults,solarAltitude:3,solarAzimuth:275,haze:.54,warmLight:.34,cloudCover:1,humidity:.96,highCoverage:.08,highOpacity:.24,midCoverage:.90,midOpacity:.96,midScale:.65,midDetail:1.22,midSoftness:.08,midEdgeLight:.22,lowCoverage:1,lowOpacity:1,lowScale:.58,lowBuild:1.75,connected:.87,baseDarkness:.88,lightLeaks:.30,contrast:1.36,windSpeed:34,windDirection:225}},
 octClearing:{date:'2026-10-12',weather:'showers',state:{...defaults,solarAltitude:-1.6,solarAzimuth:263,haze:.27,warmLight:.88,cloudCover:.58,humidity:.78,highCoverage:.16,highOpacity:.34,midCoverage:.50,midOpacity:.78,midScale:.84,midDetail:1.16,midEdgeLight:.76,lowCoverage:.30,lowOpacity:.88,lowScale:.73,lowBuild:1.30,connected:.15,baseDarkness:.46,lightLeaks:1.05,windSpeed:12,windDirection:240}}
};

const canvas=document.getElementById('sky'),panel=document.getElementById('panel'),badge=document.getElementById('badge'),readout=document.getElementById('readout'),presetNode=document.getElementById('preset'),dateNode=document.getElementById('date'),weatherNode=document.getElementById('weatherFamily');
const DPR=2;
let state={...defaults},renderer,scene,camera,geometry,material,uniforms,noiseTexture,start=performance.now(),raf=0,width=1,height=1,pageVisible=!document.hidden;
const weatherState=()=>{const family=weatherNode.value,code=WMO[family]??0;return{weatherCode:code,cloudCover:state.cloudCover,humidity:state.humidity,rain:family==='rain'?.55:family==='thunderstorm'?.86:0,showers:family==='showers'?.55:0,precipitation:(family==='rain'||family==='showers'||family==='thunderstorm')?.55:0,storm:family==='thunderstorm'?.84:0}};
const profileDate=()=>new Date(`${dateNode.value||'2026-01-15'}T18:20:00+07:00`);
const seasonalState=()=>seasonalSkyForState({date:profileDate(),solarAltitude:state.solarAltitude,solarAzimuth:state.solarAzimuth,weather:weatherState()});
const skyY=alt=>clamp(.12+clamp((alt+2)/82,0,1)*.72,.08,.88);
const setColor=(name,value)=>uniforms[name].value.set(value[0],value[1],value[2]);
function makeUniforms(){return{
 uNoise:{value:null},uTime:{value:0},uResolution:{value:new THREE.Vector2(1,1)},uSun:{value:new THREE.Vector2(.72,.55)},uMoon:{value:new THREE.Vector2(.28,.2)},uTilt:{value:new THREE.Vector2(0,0)},uTiltScale:{value:new THREE.Vector2(0,0)},uSkyTop:{value:vec3([.3,.4,.7])},uSkyHorizon:{value:vec3([.8,.6,.5])},uCloudAmbient:{value:vec3([.75,.77,.8])},uCloudWarm:{value:vec3([.92,.68,.64])},uCloudBase:{value:vec3([.45,.48,.54])},
 uSolarAltitude:{value:-3.3},uSolarAzimuth:{value:249},uMoonAltitude:{value:-90},uMoonIllumination:{value:.5},uMoonPhase:{value:.5},uTemperature:{value:30},uHumidity:{value:.58},uVisibility:{value:30},uStorm:{value:0},uFog:{value:0},uHeat:{value:0},uWind:{value:8},uWindDirection:{value:220},uPm25:{value:18},uDust:{value:0},uSmoke:{value:0},uFlash:{value:0},uSeasonalStrength:{value:1},uHaze:{value:.18},uWarmLight:{value:.92},uSunRadius:{value:.018},uSunGlow:{value:1.15},uMoonRadius:{value:.0215},uMoonGlow:{value:.72},uCelestialEnabled:{value:1},
 uHighCoverage:{value:0},uHighOpacity:{value:.28},uHighScale:{value:1.15},uHighStretch:{value:4.4},uHighSpeed:{value:.42},uMidCoverage:{value:0},uMidOpacity:{value:.5},uMidScale:{value:1},uMidDetail:{value:1},uMidSoftness:{value:.11},uMidEdgeLight:{value:.62},uLowCoverage:{value:0},uLowOpacity:{value:.82},uLowScale:{value:.85},uLowBuild:{value:1},uConnected:{value:0},uBaseDarkness:{value:.22},uLightLeaks:{value:.52},uCloudContrast:{value:1.15},uStormScale:{value:.78},uFogScale:{value:.22},uPmScale:{value:1}
}};
function build(){renderer=new THREE.WebGLRenderer({canvas,antialias:false,alpha:false,powerPreference:'high-performance',precision:'highp'});renderer.outputColorSpace=THREE.SRGBColorSpace;renderer.setPixelRatio(DPR);scene=new THREE.Scene();camera=new THREE.OrthographicCamera(-1,1,1,-1,0,1);geometry=new THREE.PlaneGeometry(2,2);uniforms=makeUniforms();noiseTexture=createAtmosphereNoiseTexture(THREE);uniforms.uNoise.value=noiseTexture;material=new THREE.ShaderMaterial({uniforms,depthWrite:false,depthTest:false,vertexShader:ATMOSPHERE_VERTEX_SHADER,fragmentShader:ATMOSPHERE_FRAGMENT_SHADER});scene.add(new THREE.Mesh(geometry,material));resize();applyUniforms()}
function resize(){width=Math.max(1,innerWidth);height=Math.max(1,innerHeight);renderer?.setSize(width,height,false);uniforms?.uResolution.value.set(width*DPR,height*DPR)}
function applyUniforms(){if(!uniforms)return;const s=seasonalState(),az=state.solarAzimuth*Math.PI/180,family=weatherNode.value;setColor('uSkyTop',s.top);setColor('uSkyHorizon',s.horizon);setColor('uCloudAmbient',s.cloudAmbient);setColor('uCloudWarm',s.cloudWarm);setColor('uCloudBase',s.cloudBase);uniforms.uSun.value.set(clamp(.5-Math.sin(az)*.42,.06,.94),skyY(state.solarAltitude));uniforms.uSolarAltitude.value=state.solarAltitude;uniforms.uSolarAzimuth.value=state.solarAzimuth;uniforms.uSeasonalStrength.value=state.seasonal;uniforms.uHaze.value=state.haze;uniforms.uWarmLight.value=state.warmLight;uniforms.uHumidity.value=state.humidity;uniforms.uStorm.value=family==='thunderstorm'?.84:family==='rain'?.34:family==='showers'?.22:0;uniforms.uFog.value=0;uniforms.uWind.value=state.windSpeed;uniforms.uWindDirection.value=state.windDirection;uniforms.uHighCoverage.value=state.highCoverage;uniforms.uHighOpacity.value=state.highOpacity;uniforms.uHighScale.value=state.highScale;uniforms.uHighStretch.value=state.highStretch;uniforms.uHighSpeed.value=state.highSpeed;uniforms.uMidCoverage.value=state.midCoverage;uniforms.uMidOpacity.value=state.midOpacity;uniforms.uMidScale.value=state.midScale;uniforms.uMidDetail.value=state.midDetail;uniforms.uMidSoftness.value=state.midSoftness;uniforms.uMidEdgeLight.value=state.midEdgeLight;uniforms.uLowCoverage.value=state.lowCoverage;uniforms.uLowOpacity.value=state.lowOpacity;uniforms.uLowScale.value=state.lowScale;uniforms.uLowBuild.value=state.lowBuild;uniforms.uConnected.value=state.connected;uniforms.uBaseDarkness.value=state.baseDarkness;uniforms.uLightLeaks.value=state.lightLeaks;uniforms.uCloudContrast.value=state.contrast;updateReadout()}
function frame(now){raf=0;if(!pageVisible)return;uniforms.uTime.value=(now-start)/1000;renderer.render(scene,camera);raf=requestAnimationFrame(frame)}
function startFrame(){if(!raf&&pageVisible)raf=requestAnimationFrame(frame)}
function autoMorphology(){const profile=bangkokSeasonalProfile(profileDate()),m=cloudMorphologyForWeather(weatherState(),profile);state.highCoverage=m.high;state.midCoverage=m.mid;state.lowCoverage=m.low;state.connected=m.connected;state.baseDarkness=m.darkness;state.midEdgeLight=.25+m.edge*.85;state.highOpacity=.30+m.high*.24;state.midOpacity=.68+m.mid*.27;state.lowOpacity=.74+m.low*.25;state.lightLeaks=weatherNode.value==='thunderstorm'?.28:weatherNode.value==='showers'?.76:.58;syncControls();applyUniforms()}
function applyPreset(name=presetNode.value){const p=presets[name]||presets.janClear;state={...defaults,...p.state};dateNode.value=p.date;weatherNode.value=p.weather;syncControls();applyUniforms()}
function format(key,v){if(key==='solarAltitude'||key==='solarAzimuth'||key==='windDirection')return`${Number(v).toFixed(key==='solarAltitude'?1:0)}°`;if(key==='windSpeed')return`${Number(v).toFixed(1)} km/h`;if(['highScale','midScale','lowScale','midDetail','lowBuild','contrast','highStretch'].includes(key))return Number(v).toFixed(2);return`${Math.round(Number(v)*100)}%`}
function syncControls(){document.querySelectorAll('[data-k]').forEach(input=>{const key=input.dataset.k;input.value=state[key];const out=input.parentElement.querySelector('output');if(out)out.textContent=format(key,state[key])})}
function updateReadout(){const s=seasonalState(),p=s.profile,m=s.morphology;badge.textContent=`${p.anchorA.replaceAll('-',' ').toUpperCase()} · ${weatherNode.value.replaceAll('-',' ').toUpperCase()}`;readout.textContent=[`profile   ${p.anchorA} → ${p.anchorB}`,`sun       ${state.solarAltitude.toFixed(1)}° alt · ${Math.round(state.solarAzimuth)}° az · ${state.solarAltitude>-.12?'disc visible':'below horizon'}`,`weather   ${weatherNode.value} · cover ${Math.round(state.cloudCover*100)}% · RH ${Math.round(state.humidity*100)}%`,`high      ${Math.round(state.highCoverage*100)}% · opacity ${Math.round(state.highOpacity*100)}% · stretch ${state.highStretch.toFixed(2)}`,`mid       ${Math.round(state.midCoverage*100)}% · opacity ${Math.round(state.midOpacity*100)}% · edge light ${Math.round(state.midEdgeLight*100)}%`,`low       ${Math.round(state.lowCoverage*100)}% · connected ${Math.round(state.connected*100)}% · base dark ${Math.round(state.baseDarkness*100)}%`,`auto ref   high ${Math.round(m.high*100)} · mid ${Math.round(m.mid*100)} · low ${Math.round(m.low*100)} · connected ${Math.round(m.connected*100)}`,`renderer   shared Phase 8.2 · DPR ${DPR}`].join('\n')}
const hashKeys=Object.keys(defaults);function saveHash(){const p=new URLSearchParams();p.set('date',dateNode.value);p.set('weather',weatherNode.value);for(const key of hashKeys)if(Math.abs(Number(state[key])-Number(defaults[key]))>.00001)p.set(key,String(state[key]));history.replaceState(null,'',`${location.pathname}?${p.toString()}`)}function loadHash(){const p=new URLSearchParams(location.search);if(p.has('date'))dateNode.value=p.get('date');if(p.has('weather')&&WMO[p.get('weather')]!==undefined)weatherNode.value=p.get('weather');for(const key of hashKeys)if(p.has(key)&&Number.isFinite(Number(p.get(key))))state[key]=Number(p.get(key))}
function bind(){document.querySelectorAll('[data-k]').forEach(input=>input.addEventListener('input',()=>{state[input.dataset.k]=Number(input.value);syncControls();applyUniforms();saveHash()},{passive:true}));dateNode.addEventListener('change',()=>{applyUniforms();saveHash()});weatherNode.addEventListener('change',()=>{applyUniforms();saveHash()});document.getElementById('applyPreset').addEventListener('click',()=>{applyPreset();saveHash()});document.getElementById('applyAuto').addEventListener('click',()=>{autoMorphology();saveHash()});document.getElementById('reset').addEventListener('click',()=>{presetNode.value='janClear';applyPreset('janClear');saveHash()});document.getElementById('copy').addEventListener('click',async e=>{saveHash();try{await navigator.clipboard.writeText(location.href);e.currentTarget.textContent='COPIED';setTimeout(()=>e.currentTarget.textContent='COPY STATE LINK',1000)}catch(_){}});document.getElementById('fullscreen').addEventListener('click',()=>document.fullscreenElement?document.exitFullscreen():document.documentElement.requestFullscreen?.({navigationUI:'hide'}));document.getElementById('hidePanel').addEventListener('click',()=>{panel.classList.add('hidden');document.getElementById('showPanel').classList.add('visible')});document.getElementById('showPanel').addEventListener('click',()=>{panel.classList.remove('hidden');document.getElementById('showPanel').classList.remove('visible')});document.addEventListener('visibilitychange',()=>{pageVisible=!document.hidden;if(pageVisible)startFrame();else if(raf){cancelAnimationFrame(raf);raf=0}})}
loadHash();syncControls();build();bind();applyUniforms();startFrame();addEventListener('resize',resize,{passive:true});
